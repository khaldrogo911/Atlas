"""Market regime classification.

Assignment of the prevailing market state — trend, range, volatility and
liquidity conditions — from features, so that downstream components can
condition their behaviour on it.

Boundary:
    Describes the market; never decides a trade.

ATLAS-TASK-0001 establishes this package as an empty, importable unit with a
declared responsibility. Its implementation is delivered by a later task.
"""

from __future__ import annotations

__all__: list[str] = []
